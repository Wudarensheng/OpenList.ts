{
  "name": "openlist-ts",
  "version": "1.0.0",
  "description": "OpenList TypeScript/JavaScript rewrite for Cloudflare Workers",
  "main": "dist/worker.js",
  "scripts": {
    "dev": "wrangler dev -c wrangler.local.toml",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute openlist-db -c wrangler.local.toml --file=./src/models/schema.sql",
    "db:reset": "wrangler d1 execute openlist-db -c wrangler.local.toml --command='DROP TABLE IF EXISTS files; DROP TABLE IF EXISTS file_cache; DROP TABLE IF EXISTS settings; DROP TABLE IF EXISTS storages; DROP TABLE IF EXISTS users;' && npm run db:init"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.540.0",
    "@aws-sdk/s3-request-presigner": "^3.540.0",
    "aws4fetch": "^1.0.20",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "@types/node": "^20.12.7",
    "@types/qrcode": "^1.5.6",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0",
    "wrangler": "^4.125.0"
  }
}
